import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emmaus.bibliomethod',
  appName: 'Emmaus BiblioMethod',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
